# 以闪资源接入 · 独立开发分支

本分支是独立的资源调查与中转工程，不修改、不合并原 main 分支，也不包含游戏资源、账户或密钥。

第一步使用有请求预算的只读探测，从国服官网提取真实下载入口与脚本引用，保存可复核证据。没有取得全量清单时不能称为全量接入。官网宣传图片不计入游戏资产。

运行：`python tools/discover.py`。输出在 `out/discovery.json`，原始网页/脚本保留在 `out/raw/`，仅用于本轮调查。

本分支未部署公共中转、未实现模型转换。所有状态以实测输出为准。
